# Exp 8 hash
def custom_hash(s):
    hash_val = 0
    for char in s:
        hash_val = 31 * hash_val + ord(char)
    return hash_val
s = input("Enter a string: ")
print("Custom hash:", custom_hash(s))
print("Python hash():", hash(s))
